"
Hover/selection overlay for SWAStSpyTreemapMorph. Uses the inherited behaviour (explore node on click); exists as a distinct class so the st-spy treemap can evolve its own menu/decorations independently.
"
Class {
	#name : #SWASamplingTallyTreemapOverlay,
	#superclass : #SWATreemapOverlay,
	#category : #'SWA-MessageTally',
	#'squeak_changestamp' : '<historical>'
}
